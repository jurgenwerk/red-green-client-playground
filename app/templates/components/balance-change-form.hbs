{{pick-a-date date=(readonly dateForInput) on-selected=(action 'setEntryDate')}}
{{focus-input value=humanInputValue enter="save"}} {{currencySymbol}}

<button {{action 'save'}}>
  save
</button>

{{#each balanceChange.errors as |error|}}
  <p>
    {{error.attribute}}:
    {{error.message}}
  </p>
{{/each}}

{{yield}}
